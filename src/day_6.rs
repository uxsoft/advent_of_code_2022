

pub fn index_of_unique_chars(n: u32, input: String)
{

}

pub fn process(input: String) {
    input.
}

#[cfg(test)]
mod tests_part6 {
    // Note this useful idiom: importing names from outer (for mod tests) scope.
    use super::*;

    #[test]
    fn test_examples() {
        assert_
    }
}
